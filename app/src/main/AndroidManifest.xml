<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.neostore">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.login.Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.login.Activity.RegistrationActivity" />
        <activity android:name=".view.home.HomeActivity" />
        <activity android:name=".view.products.Activity.ProductListingActivity" />
        <activity android:name=".view.Address.Activity.AddAddressActivity" />
        <activity android:name=".view.products.Activity.ProductDetailActivity" />
        <activity android:name=".view.Address.Activity.AddressListActivity" />
        <activity android:name=".view.Orders.Activity.MyOrdersActivity" />
        <activity android:name=".view.Orders.Activity.OrderIdActivity" />
        <activity android:name=".view.login.Activity.ForgotPasswordActivity" />
        <activity android:name=".view.Cart.MyCartActivity" />
        <activity android:name=".view.login.Activity.ResetPassword" />
        <activity android:name=".view.myProfile.MyAccountActivity" />
        <activity android:name=".view.myProfile.EditProfileActivity" />
    </application>

</manifest>